// this component should be used as is - when added to a page, wherever in the page, it *locks* the scroll on the component
// and has to scroll through the whole component before continuing on the page
