import styled from 'styled-components/macro'

export const StyledSortableList = styled.ul`
        list-style: none;
        margin:0;
        padding:0;

        li {
            cursor: pointer;
        }

        .on-drag-over{
            /* border: 10px orange solid; */
        }
`

