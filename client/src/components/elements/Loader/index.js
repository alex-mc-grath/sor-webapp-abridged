import React from 'react'
import { Style } from './Style'
import { useEffect, useRef } from 'react';
import {Loader as Loader_b} from '../../../_boilerplate/elements/Loader'

export const Loader = () => {

  return (
    <Style>
        <Loader_b />
    </Style>
  )
}

