import {Form as Form_b} from '../../../_boilerplate/inputs/Form'

export const Form = () => {
    // here is where we'll instantiate our Form component, specific to this project
    // for now I don't see what we can add/modify so I'll just import directly from boilerplate
}

