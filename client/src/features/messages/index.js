export const rac = () => {
return (
<div>messages - activatable or deactivable (depending on defined sequence)

    message stats micro dashboard or overview

    messages to attended to

    set your messages content? (here or in campaign settings .. I think in campaign settings)


</div>
)
}
